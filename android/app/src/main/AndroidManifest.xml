<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        QUERY_ALL_PACKAGES: omogoča vpogled v VSE nameščene aplikacije, ne le
        tiste, s katerimi si SentryScan deli intent-filtre. Brez tega
        dovoljenja bi PackageManager na Androidu 11+ vrnil samo peščico
        paketov (package visibility filtering). Ker aplikacijo namestiš z
        neposrednim APK (sideload), OS to dovoljenje odobri samodejno ob
        namestitvi - Google Play politika (ki bi zahtevala utemeljitev v
        Play Console) tu ne velja, ker aplikacija ni objavljena na Play.
    -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!--
        PACKAGE_USAGE_STATS je "posebno" (app-ops) dovoljenje. Deklaracija v
        manifestu ga NE odobri - uporabnik ga mora ročno vklopiti v
        Nastavitve > Aplikacije > Poseben dostop > Uporaba podatkov o
        uporabi (Usage access). Aplikacija to nastavitev odpre za uporabnika
        (glej PermissionGateService), sama pa ne more zahtevati privzetega
        runtime dialoga zanj.
    -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Za obvestilo "pregled zaključen" / dnevno opozorilo o bateriji. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Ne prosimo za CAMERA/MICROPHONE/LOCATION/SMS/CONTACTS - SentryScan
         analizira METAPODATKE drugih aplikacij (dovoljenja, nastavitve),
         zase pa namerno drži minimalen nabor dovoljenj, da se izognemo
         lastnemu "sumljivemu profilu" v analizi. -->

    <application
        android:label="SentryScan"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

</manifest>
